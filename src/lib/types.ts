/**
 * Team names that correspond to permissions in the system
 */
export enum TeamPermission {
  ADMIN = "admin",
  SITE_ADMIN = "site_admin",
  SUBMITTER = "submitter",
  APPROVER = "approver",
  PENDING = "pending",
  REPORTER = "reporter",
}
